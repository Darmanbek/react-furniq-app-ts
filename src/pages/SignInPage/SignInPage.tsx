import React from "react";
import { FormSignIn, SectionSign } from "@/components"

const SignInPage: React.FC = () => {
    return (
        <SectionSign>
            <FormSignIn />
        </SectionSign>
    );
};

export default SignInPage;
