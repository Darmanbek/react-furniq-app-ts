import React from "react";
import { SectionNotFound } from "@/components";

const NotFoundPage: React.FC = () => {
    return (
        <>
            <SectionNotFound />
        </>
    );
};

export default NotFoundPage;
